# KimiCode Entry Point

Read `AGENTS.md` first, then follow `docs/engineering_handoff_zh.md`.

Keep tests in `tests/`, standalone tools in `tools/`, logs in `logs/`, and build artifacts in `build*` directories. Do not mix temporary test or debug scripts into production source directories.

