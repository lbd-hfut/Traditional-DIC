# Tests

Put all unit tests, regression tests, temporary verification scripts, and test fixtures here.

Do not add test-only functions or debug-only entry points to `src/`, `include/`, `bindings/`, or `python/traditional_dic/`.

