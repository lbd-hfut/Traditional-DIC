# Q12 — Invocation and dependency fallback

Invoke the stable Traditional-DIC interface in this repository environment.
If the installed command is unavailable, use the documented source-tree
fallback or report the environment problem. Do not install or upgrade
dependencies without explicit user authorization.
