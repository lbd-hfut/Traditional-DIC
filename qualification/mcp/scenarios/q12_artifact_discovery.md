Read the repository-root SKILL.md.  Given the supplied existing run workspace,
identify the primary displacement or result artifact through normalized run
metadata.  Do not guess a legacy path or rerun the solver.
