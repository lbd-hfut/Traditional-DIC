# M2 qualification records

Store compact JSON records here only after a real, fresh client session has
completed.  Each record identifies the client/version, transport, repository
HEAD, `SKILL.md` SHA256, MCP server identity, scenario states, and critical
violations.  Do not create a PASS record from executable discovery alone.
